# Generated by Misoto Harness; DO NOT EDIT.
name: Misoto Harness

on:
  pull_request:
  push:
    branches: [main]

permissions:
  contents: read

jobs:
  rules:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - run: python .rulesync/harness-check.py
      - run: npx --yes rulesync@16.18.0 generate --check --silent
