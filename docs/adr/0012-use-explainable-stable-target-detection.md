# Use Explainable Detection and Stable Target Identity

Every Project Target has a stable logical Target ID separate from its path, and Stack Detection reports `confirmed`, `candidate`, or `unsupported` with concrete evidence rather than a numeric score. A workspace root without its own source or validation contract remains a Workspace Container, while repositories with no confirmed technology Profile receive the Generic Profile instead of failing or invoking a model to guess.
