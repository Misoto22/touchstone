# Bound Automatic Target Discovery

Stack Detection automatically creates Project Targets only for the repository root and members named by a supported workspace manifest; other nested manifests remain candidates for explicit confirmation, with fixtures, examples, generated trees, dependencies, virtual environments, vendors, and submodules excluded. JavaScript package evidence selects the JavaScript Profile, Node runtime evidence separately selects the Node Profile, and a changed workspace Target expands validation through declared reverse dependencies rather than directory guesses.
