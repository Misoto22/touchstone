# Scope Profile Sets to Project Targets

A repository contains one or more Project Targets, each with its own composable Profile Set, and a Loop may address one or more Targets. This prevents framework-specific paths and validation from bleeding across monorepo components without forcing each component into a separate Touchstone installation.
